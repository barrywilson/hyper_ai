#!/usr/bin/env bash
# Convenience wrapper to start the knowledge-graph REST API.
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$HERE"
exec uv run knowledge-graph-api "$@"
